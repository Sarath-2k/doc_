import 'package:flutter/material.dart';

class Bin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Deleted Files Appear here")),
    );
  }
}
