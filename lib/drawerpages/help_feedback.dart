import 'package:flutter/material.dart';

class HelpFeedback extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("If You Need Help Ping Us")),
    );
  }
}
