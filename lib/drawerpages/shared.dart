import 'package:flutter/material.dart';

class Shared extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:
          Center(child: Text("The Documents You Shared Or Are Shared To You")),
    );
  }
}
