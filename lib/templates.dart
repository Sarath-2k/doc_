import 'package:flutter/material.dart';

class Templates extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(child: Text("Choose templates From Here"),),
    );
  }
}